git_url(ENV["MATCH_GIT_URL"] || "https://github.com/IngrediCheck/ios-certificates.git")

storage_mode("git")
type("appstore") # default; can be overridden per-lane

app_identifier(["llc.fungee.ingredicheck"])

username(ENV["APPLE_ID"] || "your-apple-id@example.com")


